const header = {
    color: "#965765",
    fontSize: "40px",
}

export default function InlineComponent() {
    return (
        <div>
            <h1 style={header}> Inline components</h1>;
        </div>
    );

}